





@model TatliGezi.Models.Gallery

<div class="row">
    <div class="col-lg-12">
        @if (Model != null)
        {
            <h1 class="page-header">Gallery Düzenleme Formu</h1>
        }
        else
        {
            <h1 class="page-header">Gallery Ekleme Formu</h1>
        }

    </div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            @if (ViewBag.Durum == -1)
            {
                <div class="alert alert-danger">Ooopsss!...Lütfen Gerekli Alanları Doldurunuz.</div>
            }
            @if (ViewBag.Durum == 0)
            {
                <div class="alert alert-danger">Bu değer girilmiştir.</div>
            }

            <div class="panel-body">
                <div class="row">
                    <div class="col-lg-6">
                        @using (Html.BeginForm(null, null, FormMethod.Post, new { @enctype = "multipart/form-data" }))
                        {
                            <div class="form-group">
                                <label>Galeri Url</label>

                                @Html.TextBoxFor(x => x.Url)
                            </div>

                            <div class="form-group">
                                <label>Resim:</label>
                                <input type="file" name="resim" id="resim" />
                            </div>

                            <div class="form-group">
                                <label>Yazı Ekle</label>

                                @Html.TextBoxFor(x => x.Content)
                            </div>

                            <input type="submit" name="name" class="btn btn-primary" value="Kaydet" />

                        }

                    </div>

                </div>
                <!-- /.row (nested) -->
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>
